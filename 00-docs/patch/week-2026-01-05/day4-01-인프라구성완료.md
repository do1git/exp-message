# 패치노트 - 2026-01-08

## 목표했던 내용 1: 애플리케이션 실행 가능하도록 MySQL 설정 구성

## 변경사항 1
- `application.properties`에 MySQL 데이터소스 설정 추가 (환경변수 주입)
- `default.env` 파일 생성
- MySQL JDBC 드라이버 의존성 추가
- `bootRun` 태스크에 `.env` 파일 자동 로드 기능 추가
- Spring Boot Actuator 추가 (health check)

## 목표했던 내용 2: Docker 이미지 빌드 및 실행 환경 구성

## 변경사항 2
- `Dockerfile` 생성 (멀티 스테이지 빌드, 테스트 제외)
- `docker-compose.yml` 생성 (MySQL + App)
- `.dockerignore` 생성

## 목표했던 내용 3: Kubernetes 배포를 위한 Helm 차트 구성

## 변경사항 3
- `01-infrastructure/02-app-monolitic/` Helm 차트 생성
- `01-infrastructure/03-stack-monolitic/` Umbrella 차트 생성 (MySQL + App + Ingress)

