# Docker Registry Configuration
# Copy this file to .env and fill in the values

# Registry Settings
REGISTRY_HOST=localhost
REGISTRY_PORT=5000

# Image Settings
IMAGE_NAME=00-monolitic
IMAGE_TAG=latest

# Helm Deployment Settings
RELEASE_NAME=monolitic-stack
NAMESPACE=default
# DEPLOY_MODE: remote or local (required)
DEPLOY_MODE=local
# DEPLOY_ACTION: install, upgrade, delete, or install-or-upgrade (default: install-or-upgrade)
# DEPLOY_ACTION=install-or-upgrade

# Remote Deployment
# For remote deployment (DEPLOY_MODE=remote), you must first fetch the kubeconfig using:
# ./00-fetch-kubeconfig.sh --host HOST --user USER
# This will create remote-kubeconfig.yaml in the same directory.

